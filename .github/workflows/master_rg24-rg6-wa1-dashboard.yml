name: Build and deploy Node.js app to Azure Web App - rg24-rg6-wa1-dashboard

on:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: rg24-rg6-wa1-dashboard-production
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.x"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build --if-present

      - name: Run tests
        run: npm run test --if-present

      - name: Remove unnecessary files
        run: |
          rm -rf node_modules
          rm -rf backend/node_modules
          rm -rf frontend/node_modules
          rm -rf frontend/.next/cache
          rm -rf .git
          rm -rf .github
          rm -rf .vs
          rm -rf coverage
          find . -type f -name "*.log" -delete

      - name: Upload deployment artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: |
            .
            !.git/**
            !.github/**
            !.vs/**
            !**/node_modules/**
            !frontend/.next/cache/**
            !coverage/**
            !**/*.log
          retention-days: 1

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download deployment artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app
          path: deploy

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: rg24-rg6-wa1-dashboard
          slot-name: Production
          package: deploy
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_E369C23060DC407CB81CF489E82BE379 }}
